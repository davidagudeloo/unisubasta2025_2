import 'package:flutter/material.dart';

class PujarScreen extends StatelessWidget {
  const PujarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('pujar_screen');
  }
}
